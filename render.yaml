services:
  - type: web
    name: prédic-bot
    env: python
    plan: free
    region: frankfurt
    rootDir: app
    buildCommand: pip install -r requirements.txt
    startCommand: python -m bot.py
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: gemdb
          property: connectionString
